[package]
name = "distributed-kv"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
cineyma = { path = "../.." }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }
prost = "0.14"
bytes = "1"

[build-dependencies]
prost-build = "0.14"
